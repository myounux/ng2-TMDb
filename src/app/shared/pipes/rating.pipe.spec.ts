/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RatingPipe } from './rating.pipe';

describe('Pipe: Rating', () => {
  it('create an instance', () => {
    let pipe = new RatingPipe();
    expect(pipe).toBeTruthy();
  });
});
